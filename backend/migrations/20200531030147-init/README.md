# Migration `20200531030147-init`

This migration has been generated by Angad Singh at 5/31/2020, 3:01:47 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE `tgc-build`.`User` (
`createdAt` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP ,`email` varchar(191) NOT NULL  ,`id` int NOT NULL  AUTO_INCREMENT,`name` varchar(191)   ,`password` varchar(191) NOT NULL  ,`points` int NOT NULL DEFAULT 0 ,`updatedAt` datetime(3) NOT NULL  ,
    PRIMARY KEY (`id`)
) DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci

CREATE TABLE `tgc-build`.`Badge` (
`createdAt` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP ,`id` int NOT NULL  AUTO_INCREMENT,`img` varchar(191) NOT NULL  ,`updatedAt` datetime(3) NOT NULL  ,`userId` int  ,
    PRIMARY KEY (`id`)
) DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci

CREATE TABLE `tgc-build`.`Lesson` (
`createdAt` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP ,`end` datetime(3) NOT NULL  ,`id` int NOT NULL  AUTO_INCREMENT,`materials` varchar(191)   ,`open` boolean NOT NULL DEFAULT false ,`start` datetime(3) NOT NULL  ,`title` varchar(191) NOT NULL  ,`updatedAt` datetime(3) NOT NULL  ,`video` varchar(191)   ,
    PRIMARY KEY (`id`)
) DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci

CREATE TABLE `tgc-build`.`Attendance` (
`createdAt` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP ,`id` int NOT NULL  AUTO_INCREMENT,`lessonId` int NOT NULL ,`present` boolean NOT NULL DEFAULT true ,`updatedAt` datetime(3) NOT NULL  ,`userId` int NOT NULL ,
    PRIMARY KEY (`id`)
) DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci

CREATE TABLE `tgc-build`.`Quiz` (
`createdAt` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP ,`form` varchar(191) NOT NULL  ,`id` int NOT NULL  AUTO_INCREMENT,`lessonId` int NOT NULL ,`max` int NOT NULL  ,`updatedAt` datetime(3) NOT NULL  ,
    PRIMARY KEY (`id`)
) DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci

CREATE TABLE `tgc-build`.`UserQuiz` (
`createdAt` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP ,`id` int NOT NULL  AUTO_INCREMENT,`quizId` int NOT NULL ,`score` int NOT NULL  ,`updatedAt` datetime(3) NOT NULL  ,`userId` int NOT NULL ,
    PRIMARY KEY (`id`)
) DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci

CREATE UNIQUE INDEX `User.email` ON `tgc-build`.`User`(`email`)

ALTER TABLE `tgc-build`.`Badge` ADD FOREIGN KEY (`userId`) REFERENCES `tgc-build`.`User`(`id`) ON DELETE SET NULL ON UPDATE CASCADE

ALTER TABLE `tgc-build`.`Attendance` ADD FOREIGN KEY (`lessonId`) REFERENCES `tgc-build`.`Lesson`(`id`) ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE `tgc-build`.`Attendance` ADD FOREIGN KEY (`userId`) REFERENCES `tgc-build`.`User`(`id`) ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE `tgc-build`.`Quiz` ADD FOREIGN KEY (`lessonId`) REFERENCES `tgc-build`.`Lesson`(`id`) ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE `tgc-build`.`UserQuiz` ADD FOREIGN KEY (`quizId`) REFERENCES `tgc-build`.`Quiz`(`id`) ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE `tgc-build`.`UserQuiz` ADD FOREIGN KEY (`userId`) REFERENCES `tgc-build`.`User`(`id`) ON DELETE CASCADE ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20200531030147-init
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,77 @@
+datasource db {
+  provider = "mysql"
+  url      = env("MYSQL_URL")
+}
+
+generator client {
+  provider = "prisma-client-js"
+}
+
+model User {
+  id         Int          @id @default(autoincrement())
+  createdAt  DateTime     @default(now())
+  updatedAt  DateTime     @updatedAt
+  email      String       @unique
+  password   String
+  name       String?
+  points     Int          @default(0)
+  badges     Badge[]
+  attendance Attendance[]
+  quizzes    UserQuiz[]
+}
+
+model Badge {
+  id        Int      @id @default(autoincrement())
+  createdAt DateTime @default(now())
+  updatedAt DateTime @updatedAt
+  img       String
+  User      User?    @relation(fields: [userId], references: [id])
+  userId    Int?
+}
+
+model Lesson {
+  id         Int          @id @default(autoincrement())
+  createdAt  DateTime     @default(now())
+  updatedAt  DateTime     @updatedAt
+  title      String
+  open       Boolean      @default(false)
+  start      DateTime
+  end        DateTime
+  materials  String?
+  video      String?
+  attendance Attendance[]
+  quiz       Quiz[]
+}
+
+model Attendance {
+  id        Int      @id @default(autoincrement())
+  createdAt DateTime @default(now())
+  updatedAt DateTime @updatedAt
+  lessonId  Int
+  lesson    Lesson   @relation(fields: [lessonId], references: [id])
+  userId    Int
+  user      User     @relation(fields: [userId], references: [id])
+  present   Boolean  @default(true)
+}
+
+model Quiz {
+  id        Int        @id @default(autoincrement())
+  createdAt DateTime   @default(now())
+  updatedAt DateTime   @updatedAt
+  lessonId  Int
+  lesson    Lesson     @relation(fields: [lessonId], references: [id])
+  form      String
+  max       Int
+  UserQuiz  UserQuiz[]
+}
+
+model UserQuiz {
+  id        Int      @id @default(autoincrement())
+  createdAt DateTime @default(now())
+  updatedAt DateTime @updatedAt
+  quizId    Int
+  quiz      Quiz     @relation(fields: [quizId], references: [id])
+  score     Int
+  userId    Int
+  user      User     @relation(fields: [userId], references: [id])
+}
```


